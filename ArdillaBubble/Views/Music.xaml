<navigation:Page x:Class="ArdillaBubble.Views.Music" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
           d:DesignWidth="640" d:DesignHeight="480"
           Title="Music Page">
    <Grid x:Name="LayoutRoot">
        <MediaElement HorizontalAlignment="Left" Height="100" Margin="164,200,0,0" VerticalAlignment="Top" Width="100"/>
   
        <TextBox Margin="38,33,40,49" Grid.Row="2">
            <TextBox.BitmapEffect>

                <!-- <BitmapEffectGroup> would go here if you wanted to apply more 
             then one effect to the TextBox. However, in this example only  
             one effect is being applied so BitmapEffectGroup does not need  
             to be included. -->

                <!-- The OuterGlow is blue, extends out 30 pixels, has the 
             maximum noise possible, and is 40% Opaque. -->
                <OuterGlowBitmapEffect GlowColor="Blue" GlowSize="30" Noise="1"
         Opacity="0.4" />

            </TextBox.BitmapEffect>
        </TextBox>
    </Grid>
</navigation:Page>
